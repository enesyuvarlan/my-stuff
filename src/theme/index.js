import { createTheme } from '@mui/material/styles';

let theme = createTheme({
    palette: {
        primary: {
            main: '#ecf0f1',
        },
        secondary: {
            main: '#7f8c8d',
        },
    },
});

export default theme;